<h1>Редактирование профиля:</h1>

<%= form_with model: current_user do |form| %>
  <p><%= form.label :name, "Ваше имя" %></p>
  <p><%= form.text_field :name %></p>

  <p><%= form.submit 'Сохранить' %></p>
<% end %>
<p><%= button_to 'Удалить профиль', user_path(current_user), method: :delete %></p>
<p>-----------------------------------------------</p>
<p><%= link_to 'Вернуться на мою страницу', user_path(current_user) %></p>
